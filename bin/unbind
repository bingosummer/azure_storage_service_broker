#!/bin/bash

set -e

bin=$(dirname $0)

instance_id=`cat $bin/instance_id`
binding_id=`cat $bin/binding_id`

http --auth-type basic --auth $authUsername:$authPassword DELETE 127.0.0.1:8001/v2/service_instances/$instance_id/service_bindings/$binding_id
